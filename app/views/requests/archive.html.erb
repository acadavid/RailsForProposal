<% title "Archivar RFP" %>

<%= form_for @request, :url => project_archive_path(@request.id) do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label t(:feedback) %><br />
    <%= f.text_area :feedback %>
  </p>

  <p><%= f.submit t(:save) %></p>
<% end %>
